
server:
  port: 7001
spring:
  mvc:
    pathmatch:
      matching-strategy: ant_path_matcher
  datasource:
    druid:
      driver-class-name: com.mysql.cj.jdbc.Driver
      url: jdbc:mysql://rm-2vc8850942200v94mpo.mysql.cn-chengdu.rds.aliyuncs.com:3306/submission.user
      username: root
      password: Iwillbethebest1029&&
  redis:
    host: 47.108.53.218
    port: 6379
    database: 1
management:
  endpoints:
    web:
      exposure:
        include: "*"
feign:
  okhttp:
    enabled: true
  client:
    config:
      default:
        connectTimeout: 5000
        readTimeout: 5000
        loggerLevel: basic


mybatis-plus:
  global-config:
    db-config:
      id-type: assign_uuid
  mapper-locations:
    - classpath:mapper/*.xml


# Sa-Token配置
sa-token:
  # token名称 (同时也是cookie名称)
  token-name: Authorization
  # token风格
  token-style: uuid
  # 是否输出操作日志
  is-log: true
  # 是否从cookie中读取token
  is-read-cookie: false
  # 是否从head中读取token
  is-read-header: true
